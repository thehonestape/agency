import React from 'react';
import { ComponentShowcase } from '@/components/examples';

export default function DemoPage() {
  // Directly render the ComponentShowcase with the sidebar TOC
  return <ComponentShowcase />;
} 