/**
 * Fonts configuration file
 * 
 * This exposes CSS variables for the fonts that can be used in the application.
 * The actual font files should be imported in your CSS.
 */

// Font family variables
export const fontVariables = {
  maisonNeue: 'var(--font-maison-neue)',
  maisonNeueMono: 'var(--font-maison-neue-mono)',
};

// Default font weights
export const fontWeights = {
  regular: 400,
  medium: 500,
  bold: 700,
}; 