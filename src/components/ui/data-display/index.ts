export { 
  Card, 
  CardHeader, 
  CardTitle, 
  CardDescription, 
  CardContent, 
  CardFooter,
  cardVariants,
  headerVariants,
  titleVariants,
  descriptionVariants,
  contentVariants,
  footerVariants,
} from "./card";

// Semantic data display composition
export const DataDisplay = {
  Card,
} as const; 